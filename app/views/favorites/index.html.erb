<h1>保存したレシピ一覧</h1>

<% if @recipes.any? %>
  <ul>
    <% @recipes.each do |recipe| %>
      <li>
        <%= link_to recipe.title, favorite_path(recipe) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>保存したレシピはありません。</p>
<% end %>
