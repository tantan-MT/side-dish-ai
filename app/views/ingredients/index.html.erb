<h1>食材一覧</h1>

<ul>
  <% @ingredients.each do |ingredient| %>
    <li>
      <% if ingredient.image.attached? %>
        <%= image_tag ingredient.image, width: 150 %><br>
      <% end %>

      <%= ingredient.name %>
      （<%= ingredient.ingredient_category.name %>）
    </li>
  <% end %>
</ul>
